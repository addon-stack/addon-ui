export enum ButtonVariant {
    Contained = "contained",
    Outlined = "outlined",
    Text = "text",
}

export enum ButtonColor {
    Primary = "primary",
    Secondary = "secondary",
    Accent = "accent",
    Error = "error",
    Success = "success",
}

export enum ButtonSize {
    Small = "small",
    Medium = "medium",
    Large = "large",
}

export enum ButtonRadius {
    Small = "small",
    Medium = "medium",
    Large = "large",
    Full = "full",
}
